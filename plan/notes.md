# Notes: 复杂度治理与 Provisional 清账

## 当前已确认事实

- `docs/08_reviews/` 根目录当前多出 `26_architecture_complexity_marking.md`，与“活跃入口只保留 `21 / 24 / 25 / archive`”的规则冲突。
- `docs/02_p1_architecture/02_pdcp_system_architecture_review_package.md` 当前规则角色是阶段评审包，而不是并列主入口。
- `03_decision_log.md` 中的 `provisional` 命中包含历史留痕，不能直接等同于活跃复杂度债。
- archive 目录规则要求保留原始正文与历史语境，不适合批量重写旧路径。

## 待 Agent Team 返回的关键问题

- `26` 归档时，最小同步面包含哪些 README / 索引 / 说明。
- 当前真正需要 Linear 承接的活跃 provisional / orphan 集合。
- 当前 Linear 项目中哪些 issue 应关闭、拆分、保留或新增。
- guardrails 应写入 `AGENTS.md` 还是 `01_workflow.md`。
