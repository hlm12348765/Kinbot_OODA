# P4 Beta 与 DVT

收纳 Beta 试点、DVT 验证和 MVP 验收计划相关文档。

## 目录角色

用于定义中后期验证范围、试点框架和放行规则。

## 文档索引

- `01_mvp_validation_plan.md`：量产预备与MVP验证计划。核心主题：MVP 范围、验证域、试点框架与放行规则。

## 维护规则

1. 文档内容继续使用中文撰写。
2. 文件名使用英文小写、数字前缀和生命周期目录组织。
3. 目录级变更统一只在根目录 `CHANGELOG.md` 中记录，不再维护子目录 `CHANGELOG.md`。
4. 当目录内新增文档时，需同步回写本 `README.md` 的文档索引。
