# AGENTS.md

---

文档版本：v2.1
创建日期：2026-03-21
作者：Codex-架构师

文档变更记录：
- v2.1 | 2026-07-22 | Codex-架构师 | 新增通用决策推理方法：把命名二选一还原为连续变量，以少数决策轴逐组件代入，通过纯粹极端的可验证失效场景反推混合边界，并纳入记号复用、破例审查和组织目标函数。
- v2.0 | 2026-07-20 | Codex-架构师 | 按渐进式披露原则拆分根规则：根文件只保留通用约束和路由，文档、架构、研究、招聘及仓库工作流下沉到对应目录。

> v1.64 及以前的完整变更记录与规则原文保存在 `docs/00_governance/agent_rules/AGENTS_v1.64_full_archive.md`，仅用于迁移核对和历史追溯。

---

## 1. 本文件的职责

本文件只保留所有任务都必须遵守的规则，以及按任务加载详细规则的入口。不要把专项流程、候选人状态、论文清单、检查命令或当前架构细节继续堆回根文件。

项目当前处于产品需求基本完成后的系统架构设计与技术研判阶段，主线为 `P1 / PDCP`，目标是形成系统架构、总体方案和模块方案基线。当前量产预备目标为 `2026-12-31`，整机 BOM 基线为 `5000～6000 元`。

## 2. 事实源优先级

判断当前有效事实时，按以下顺序：

1. 用户在当前对话中的最新明确指令。
2. `input/00_requirements/00_user_requirements_input.md`。
3. `docs/00_governance/03_decision_log.md`。
4. `docs/00_governance/01_workflow.md`。
5. `docs/00_governance/02_lifecycle_workflow_and_gates.md`。
6. 当前主线架构与方案文档。
7. 评审文档与研究文档。
8. 代理自己的推断。

高优先级与低优先级冲突时以前者为准；不得把历史推断置于用户输入之上。真实仓库状态与文档不一致时，先核对事实，再回写文档。

## 3. 编辑边界

除非用户明确要求，不得修改：

- `input/00_requirements/00_user_requirements_input.md`
- `CLAUDE.md`

默认可修改当前任务需要的正式文档、目录索引、根 `README.md`、根 `CHANGELOG.md` 和各级 `AGENTS.md`。

工作区可能包含用户或其他线程的修改：

- 不得无说明覆盖、回退或删除。
- 只修改当前任务所需文件；有重叠时先读差异并兼容现状。
- 引用其他线程的专项结论时标明来源。
- 输入目录只保存输入，正式结论写入相应的评估或主线文档。

## 4. 按任务加载规则

只读取与当前任务相关的规则：

| 任务范围                             | 需要读取                                                                  |
| -------------------------------- | --------------------------------------------------------------------- |
| 维护 `docs/` 下任意正式文档               | `docs/AGENTS.md`                                                      |
| 系统架构、P1、递归 Agent、SysML 架构模型      | `docs/02_p1_architecture/AGENTS.md`                                   |
| 论文、arXiv、VLN / NFM、前沿技术研究        | `docs/09_research/AGENTS.md`                                          |
| 团队规划、岗位、简历、候选人和面试                | `docs/10_team_planning/AGENTS.md`                                     |
| Linear、Git、输入归档、输出交付、计划、日志或 hook | `docs/00_governance/agent_rules/repository_and_delivery_workflows.md` |
| 核对拆分前规则是否遗漏                      | `docs/00_governance/agent_rules/AGENTS_v1.64_full_archive.md`         |

更深目录的 `AGENTS.md` 只补充本目录规则，并继承上层规则。历史归档不是默认必读项。

## 5. 通用工作方式

- 先明确任务属于回答、诊断、评审还是修改；没有修改授权时只做分析。
- 先读当前事实源和真实文件，再形成判断。
- 解释问题时先说原因，再说解决办法。
- 使用自然、准确的中文；优先采用日常用语或有明确含义的专业术语。
- 新结论必须区分已确认事实、候选方案和代理推断。
- 用户已批准的路线应推进到下一个必须审批的阶段门，不反复请求继续授权。
- 只有需要用户做新的重大判断、阶段门批准、风险取舍或扩大权限时才暂停。
- 重要文档修改完成后，按影响范围检查目录索引、`CHANGELOG.md`、决策记录和项目管理状态。

## 6. 核心决策方法：从二选一到可验证边界

这套方法适用于架构、技术路线、流程和组织边界。`Agentic System` 只作为说明方法的例子，不因此升级为已确认架构事实。

1. **先找底层连续变量，不直接回答“A 还是 B”。** 两个命名模式通常是同一变量的不同取值。先问真正可调的量是什么。以 `Leader + Area Agent` 和 `Master + Skill` 为例，底层问题不是哪个名字更先进，而是“在哪个粒度切开责任，以及切口处用什么语言通信”。
2. **只保留 `2～4` 条真正驱动决策的轴，不列通用优缺点。** 知道这些轴的取值后，应能推出边界选择。Agentic 架构至少要检查延迟 / 安全硬约束程度，以及局部决策所需的跨域上下文量；只有确实改变答案时才增加其他轴。
3. **把决策轴逐一代入具体能力域。** 不得停在抽象框架或多智能体通论；必须落到感知、导航、操作 / 执行、语音与交互等实际模块，说明每个模块在各轴上的取值和由此推出的责任切口。
4. **把每个选项推到纯粹极端，用具体失效场景论证。** 结论必须是可验证或可推翻的主张，而不是“有利有弊”。例如，需要验证纯 `Leader + Area Agent` 是否会因跨域上下文不足造成语言与动作不一致，也要验证纯 `Master + Skill` 是否会让安全场景依赖不可接受的中心闭环延迟；这些是待证伪假设，不是预设结论。
5. **混合方案只能从失效场景反推。** 每一条分层或混合边界都必须回答：“它具体阻断了第 4 步的哪个失效场景？”答不出来的边界就是无理由的复杂度，不得用“各取所长”代替论证。
6. **复用已有记号和结构，不另起体系。** 系统架构具有自相似性；同类责任、状态、接口和约束应沿用已建立的术语、视图和分解规则。只有原记号无法表达新语义时才扩展，并说明对应关系。
7. **主动寻找方案的破例情况。** 明确在哪些模块、场景、负载、故障或阶段下当前规则不成立；说明破例是由哪个轴越界造成，以及采用例外、降级还是重新切分。不得把例外藏在笼统的“后续优化”中。
8. **把组织约束写进目标函数。** 团队边界、发布节奏、所有权、验证能力、迁移成本和协作接口都属于设计输入。所谓“技术最优”必须说明相对于什么目标函数；无法被当前团队实现、验证和按节奏发布的方案，不得称为当前最优。

使用这套方法形成结论时，最低输出应包含：底层连续变量、`2～4` 条决策轴、逐能力域映射、两端纯粹方案的具体失效场景、每条混合边界对应阻断的失效场景、破例情况，以及组织目标函数。缺少其中任何一项，只能视为分析框架，不能视为完成决策。

## 7. 版本与协作安全

- 修改前检查工作区状态和目标文件差异。
- 不使用破坏性 Git 操作，不强推，不覆盖其他线程成果。
- 不把临时文件、本地日志、敏感输入或 Finder 噪声混入正式提交。
- 未经用户要求，不自行提交、推送、关闭 Issue 或改变外部系统状态。

## 8. 输出要求

与用户协作时应直接说明：判断、原因、影响和下一步。暂停审阅时必须说清要审哪份内容、重点看什么、本轮要确认什么，不能只说“请看一下”。

## 9. 维护原则

- 根文件只允许增加所有任务都需要的稳定规则或新路由，不承载专项知识。
- 专项规则优先写入对应目录的 `AGENTS.md`；低频操作流程写入按需读取的治理文档。
- 候选人状态、论文结果、架构当前细节等易变事实应留在各自事实源，不复制进根文件。
- 每次新增规则前先判断：能否改写现有规则、能否放入既有事实源、是否只适用于一个目录。
- 定期检查重复、过期、无承接和可由检查工具替代的规则。
